export interface Post {
  id: string;
  title: string;
  description: string;
  image: string;
  tag: string;
  createdAt: string;
}

export interface Notification {
  id: string;
  title: string;
  subtitle: string;
}
